'use strict';

const number = Math.trunc(Math.random()*20)+1;

let guess = document.querySelector('.check').addEventListener('click', function(){
    guess = Number(document.querySelector('.guess').value);
    // console.log(typeof guess);

    
});

